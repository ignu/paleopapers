# Use the README file as a reference to customize settings.

Paper:
  fields:
    default: article_url, pdf_url, title, description, quote, tag_id
    form: article_url, pdf_url, title, description, quote, tag_id
  relationships: 
  application: Application
